#[test]
fn t1() {
    // 记录匹配到哪儿了
    let match_idx = 0;
    let p = "s.*sfa";
    let matched = false;
    // 遍历s匹配
    for c in s:
        if match_idx == p.len() - 1:
            matched = 
        if c == p[match_idx]:
            if p[match_idx + 1]
        
}